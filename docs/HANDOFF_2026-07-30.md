# Clozn autonomous-run handoff — 2026-07-30

Authoritative record of the BK-away autonomous run. Companion to
[HANDOFF_2026-07-29.md](HANDOFF_2026-07-29.md), which covers the preceding waves.

**Repository state at writing:** `main` == `origin/main` == `c2c9665`, working tree clean, everything
merged and pushed. Suite **5138 passed / 18 skipped / 5 subtests**, cheap gate 51 OK, Studio vitest
**76 passed / 13 files**, `tsc` clean, bundle **595 KB** (0 test-tooling matches). **CI is green** --
see section 0; it was not, for the ten days before this.

*(An earlier revision of this header read `1c8d837` / 4468 passed, from before the A-F closeout wave
below. Superseded, not deleted, so the delta stays legible.)*

Live batteries, all green against real GGUFs on this box: engine exactness **20/20**, checkpoint-pin
**22/22**, gateway fork **13/13**, EOS boundary **3/3**, managed runtime **20 PASS**.

### How to run the gates (authority: `.github/workflows/ci.yml`)

    pytest tests/ clozn/ -q                                              # full product suite
    python -m unittest tests.test_runtime_architecture tests.test_product_smoke   # "cheap gate", 51 tests
    ctest --test-dir engine/core/build-cpu --output-on-failure           # native

`pytest tests/` ALONE undercounts — inline tests live under `clozn/` too. Bare `pytest` from the repo
root is wrong in the other direction: it sweeps `engine/core/third_party/llama.cpp` and the kernel
reference tests, which error at collection without their own deps. Both mistakes were made during this
run; the CI invocation above is the only one that means anything.


---

## 0. CI WAS RED FOR TEN DAYS -- read this first

`ci.yml` on `main` last passed **2026-07-20**. During the closeout wave below, six increments were
merged and pushed with "verified, suite green" in their commit messages, every one of them from LOCAL
runs on the Windows dev box. **CI was never checked.** The local numbers were true; the confidence
attached to them was not earned.

Three bugs were stacked, each hiding the next:

1. **`pytest` was never installed** (`efda560`). The install step installs torch, transformers,
   tokenizers, numpy, openai and ollama -- not pytest. The next step runs `pytest tests/ clozn/ -q` and
   died at `command not found`. The step named "primary gate" had not executed in ten days. The comment
   directly above it even lists pytest as part of the minimal set.
2. **Two runtime-boundary tests needed an engine binary no CI job builds** (`b1d8766`). `spawn_runtime`
   resolves the engine BEFORE reaching anything those tests stubbed. Fixed by stubbing discovery, **not**
   skipping -- a skip would have gone green just as fast while leaving two architectural invariants
   verified only on a dev box that happens to have a build, which is exactly how they rotted.
3. **The kernel step was a COLLECTION error** (`551b6b5`). `clozn_lab` was deleted by `95168c2`
   ("remove the diffusion program") but six parity tests still imported it at module scope, taking 19
   unrelated numpy tests down with them. Guarded with explicit skips, not deleted -- **those 6 tests are
   dead and await a human decision.**

CI then immediately earned its keep: the POSIX-scripts merge went red within minutes, because two new
tests asserted `LD_LIBRARY_PATH` was absent without ever controlling it, and GitHub's Linux runners
export it.

**Lesson: local green is not green. `gh run list --branch main` costs one command.**

---

## 0b. Corrections to claims made during the closeout wave

Same discipline as section 2. Each of these was asserted confidently and turned out wrong.

1. **"clozn is Windows-only" -- FALSE.** `clozn/setup/platform_detect.py` already detects
   windows/linux/macos plus arch and treats Apple Silicon as Metal-capable; `clozn.sh` is a POSIX
   launcher. The owner had already run it on a Mac. The real gap was only POSIX *convenience build
   scripts* plus Windows-shaped library-path plumbing that was INERT, not broken, on macOS. Worse: BK's
   own July commit message said the codebase "was already structurally cross-platform" -- that line was
   quoted and then contradicted inside the same brief.
2. **"C3 span removal is section-aligned only" -- FALSE.** `clozn/receipts/span_receipt.py` already did
   character-offset ablation. It was simply unreachable, imported by nothing outside its own tests. The
   real gap was the stable-address-to-offset translation layer.
3. **"An HTTP handler exception can kill the process that owns the workers" -- FALSE.**
   `ThreadingHTTPServer` isolates per-thread exceptions and `app.py` catches `Exception` -> 500 at
   `do_GET:1119` and `_dispatch_post:1255`. The real loss from merging processes is narrower: today a
   *sibling* notices gateway death via `gateway.poll()` and tears down every worker.
4. **"Widening a released schema's enum needs special justification" -- FALSE.**
   `clozn/schemas/__init__.py:23` documents enum widening as a sanctioned backward-compatible change.
5. **"The CLI has a `lab` subcommand"** -- it does not. `clozn/cli/commands/lab.py` does not exist and
   nothing registers that verb; the `app.py` docstring referencing it is stale. (Written this way
   deliberately: `scripts/docs/check_drift.py` fails the suite if user-facing docs spell out the
   removed invocation, and it cannot tell documenting-an-absence from recommending-a-command. It caught
   this line on the first run -- correctly.)
6. **The suite baseline is `pytest tests/ clozn/ -q`, not `pytest tests/`.** The latter passes cleanly
   while silently skipping ~310 inline tests -- the dangerous kind of wrong, because it is green.

---

## 0c. What shipped in the A-F closeout wave (2026-07-30)

| item | commit | note |
|---|---|---|
| C4 ask-another-question | `98a4e4c` | `unavailable` without a `reason` is a TypeScript **build error** |
| F3 conversation view | `87f4632` | chat replay is IMPOSSIBLE -- the wire format carries only 90-char summaries |
| managed run-scoped routing | `f71101a` | **found 2 pre-existing bugs**: silent wrong-model analysis in causal-trace/provenance, and a latent schema crash on snapshot-pin refusals |
| Studio preview generator | `c5b1932` | 19 states / 8 surfaces / both themes, one 534 KB file, no server |
| C3 route | `6e5d3a0` | **found: 3 of 5 intervention kinds refused on EVERY real run** |
| E4 second opinion | `3b83bc9` | no top-level success field exists; no logprob field exists |
| F5 Teach Once core | `407d779` | `resolve_corrections()` takes NO content param -- relevance matching would need a signature change |
| span-bridge fix | `8998136` | C3 works on real runs now; the content-hash guard catches the literal-redaction drift case |
| ADR 008 | `a2f4525` | design only; corrects four of the briefing claims above |
| F6 teaching loop | `c2c9665` | promotion carries the run-id PAIR, never a boolean |

**Every unblocked backend feature in the A-F arc is now complete.**

---

## 0d. Open -- each of these needs a human, not more agent-hours

* **ADR 008 decision.** The owner chose the process merge. Recommendation: proceed, but land the
  parent-death guard first. **The counter-argument:** the guard covers Linux and Windows in pure Python,
  but **macOS has no kernel equivalent** -- a real fix needs `getppid()` polling inside the C++ worker.
  ADR 006's control-channel alternative has no such gap on any platform. If macOS parity matters, that
  tips the decision back.
* **No parent-death guard exists today, at all.** SIGKILL the supervisor right now and the gateway and
  every worker orphan, holding VRAM with nothing to reap them. Four stranded-GPU incidents already.
  Worth building whichever architecture wins.
* **6 dead kernel parity tests** -- guarded with skips; deleting them is a human call.
* **Branch/worktree backlog** -- 53 worktrees, only 8 carrying unmerged commits, all 249-421 behind.
  Triage table lives in the (gitignored) roadmap notes. Nothing deleted.
* **`/v1/completions` retirement** -- M1 audit done (ADR 007, recommends a typed 410); M2 removal is
  outward-facing and unapproved.
* **Q1-Q8 self-serve qualification** -- a separate program, larger than everything above combined.
* **NOTHING VISUAL HAS BEEN SEEN BY A HUMAN.** Nine surfaces now. Section 5 still stands, but
  `npm run preview:surfaces` makes reviewing them cheap: one self-contained file, both themes, no server.

---

## 1. What shipped

Against the original three-feature plan, **Milestones A through F are complete**.

| Area | Landed |
|---|---|
| Exact execution forks | Engine route, regime-scoped exactness, gateway capture/planner/executor, mandatory unchanged-control, `/runs/<id>/fork` as an honest three-outcome wrapper, Studio surfacing |
| Durable checkpoints | export/import/truncate, fail-closed on ten identity fields *then* prove-load, content-addressed pin store, `clozn snapshot pin\|unpin\|list` |
| Mechanistic diff | pair-compatibility contract, capture + divergence map, five-arm controlled transplant, causal bisect with honest verdicts, restoration metrics, target resolver, `diff-model --mechanistic`, `experiment explain-cell` |
| Token workbench | `clozn.token-workbench.v1` read endpoint (computes nothing), four action endpoints (cached/job/unavailable), workbench shell, generic action tray |
| Evidence surfaces | run investigation, stable text-span addresses, received-context panel, "What mattered?" heatmap |
| Managed runtime | preloaded multi-model routing, per-worker concurrency (RT-05), cold-load library + loader seam (RT-04), user documentation |
| Research | quant-regression population study, mixed-precision band experiment, task-specificity bounded negative |

---

## 2. Corrections to earlier claims — read this before trusting older commit messages

Several things I asserted during the run turned out wrong and were corrected in later commits. If you
read only the earlier message, you will be misled.

1. **RT-04 cold load is not reachable from `clozn serve`.** I first said it was "latent until RT-05
   removed the global gate". Wrong cause. `ProjectionFileRouter` had no `loader=` parameter and
   `app.py` never passed one. A seam now exists; the remainder is blocked by a **process boundary**
   (§4).
2. **RT-04's eviction in-flight safety is vacuous.** I claimed it "consults REAL in-flight state via
   `WorkerHandle.track_call`". Eviction *consults* `handle.busy`, but nothing in production ever
   *enters* `track_call`, so every worker always looks idle. Fix in flight at writing.
3. **The "worker protocol has no request id" cancellation claim is overstated.** A worker-minted
   request id exists and *is* cancellable via `POST /cancel` — but only for the single active
   generation stream, not score/harvest/intervene/checkpoint or a cold load's spawn+handshake, and the
   token lives in gateway memory unreachable from the supervisor. The limitation survives; the stated
   reason was wrong.
4. **The task-specificity result is a bounded negative, not "inconclusive".** I read a
   `k_min_detected: null` as a power ceiling. It was the output of a **misspecified permutation null**
   built from only hit-bearing disagreements. After two fixes the test has demonstrated power
   (MDE 5/14 = 35.7%).

---

## 3. Research results

**Population study (n=30, controlled).** Q2_K disagrees with Q8_0 on 84.9% of prompts / 23.8% of 2384
scored positions; Q4_K_M on 42.6% / 7.7%. `structured_json` is by far the most robust category
(40.0% / 6.2%) — constrained low-entropy output survives quantization far better than free-form.
MLP-path bisects beat the random control in 20/30 cases, attention in 6/30. Depth gradient monotonic:
early 20.6% / mid 33.2% / late 53.1%.

**Mixed-precision bands (the headline).** Late-MLP band transplants restore **60%** of live
disagreements; matched early **20%**, mid **16%**. The paired comparison carries the claim, not the
marginal rates: **10 disagreements where late restored and early did not, 0 the other way; 12 vs 1
against mid.** Random equal-norm at the late band restored **0/25**.

*External corroboration* — the reason this outranked other research items: llama.cpp's own
`use_more_bits` already upgrades Q4_K_M's `ffn_down` to Q6_K at layers
{0,1,2,5,8,11,14,17,20,23,24,25,26,27}, while Q2_K's `ffn_down` is uniformly Q3_K with no depth
variation. Q4_K_M protects several late ffn layers and disagrees on 42.6% of prompts; Q2_K protects
none and disagrees on 84.9%. The shipped quantizer heuristics already encode something like this.

*Ceiling, stated in the doc:* transplanting exact reference activations is an **upper bound** on what
mixed-precision quantization could recover — not the same as quantizing those layers differently.

**Task specificity (bounded negative).** No layer restores one capability but not another:
`max_purity` p=0.336, `chi2_sum` p=0.352, bounded by a demonstrated 35.7% detection floor. So the
population study's task-independence reading — the reason late-layer recurrence looks like quantizer
geometry rather than circuitry — is **plausible but not statistically established** at the available n,
and is now qualified in that doc.

---

## 4. Open risks and blocked work

* **Cross-process cold load** — `docs/design/006-cross-process-cold-load-protocol.md` is the decision
  record. The gateway is a separate OS process from the supervisor that owns worker lifecycle; a loader
  in the gateway must either import `clozn.cli` into `clozn.server` (forbidden) or run a second,
  disconnected registry that spawns duplicate workers. **Do not wire cold load without also fixing the
  eviction vacuity** — an unreachable feature is a nuisance; a reachable one that evicts mid-generation
  corrupts a run.
* **Managed-mode surfaces fail closed for every run**, not just edge cases: receipts, replay,
  influence, and legacy fork all call plain `active_sub(h)`, which returns `None` under a managed
  gateway. Only execution-fork and snapshot pin resolve the model per run.
* **`/v1/completions` retirement** — never done, compatibility route still live.
* **Pre-existing branch/worktree backlog** — `feat/batched-sweep` (2 ahead),
  `feat/platform-macos-linux` (1), and six `worktree-agent-*` branches (1–16 ahead). **Not triaged, not
  deleted — this needs a human decision.** Merging a 342-commit-stale branch already cost real work
  this run when its premise had moved underneath it.
* `notes/UX_INFORMATION_ARCHITECTURE.md` §7.6 (casting spec) remains unimplemented in the React Studio.
* `docs/PRODUCT_ROADMAP.md` still carries two overlapping status headers.

---

## 5. NOTHING VISUAL HAS BEEN SEEN BY A HUMAN

Every Studio surface shipped in this run was verified only by vitest, `tsc`, a copy linter, and a
production build. Those prove it compiles and behaves. **They cannot prove any of it is legible or
usable.** Specific unknowns, recorded per merge:

* fork-outcome panel — layout/spacing in both themes, compact width, 200% zoom, reduced motion,
  `color-mix()` badge contrast
* workbench shell + action tray — tray layout inside the inspector aside at real viewport widths;
  possible overflow of long finding sentences in reused single-line comparison rows
* "What mattered?" heatmap — sticky row/column headers under two-axis scroll, whether 18px cells read
  at real density, dark-theme contrast of the intensity ramp, usability for a run with hundreds of
  answer-token columns, and whether both slot panels independently fetching `/investigation` reads as
  sluggish

This is the first thing to review.

---

## 6. Process lessons worth keeping

* **An unexpectedly good result is the best evidence the instrument is broken.** One result was
  retracted mid-run when all 87 random-control arms turned out to share a single frozen direction
  (`random.Random(seed)` constructed fresh per call is not independent sampling across calls). It was
  caught only because the number beat a hard-won prior.
* **Writing user documentation is a verification technique.** An agent tasked with "here is how you use
  this" traced the real end-to-end path and found that RT-04 was unreachable — something two
  implementing agents had missed.
* **Removing a lock exposes what it was hiding.** Deleting the global POST gate surfaced a 5–12%
  race in run-store schema init. Root cause was not the migration DDL but `PRAGMA journal_mode=WAL`,
  a one-time on-disk format conversion that concurrent first-time openers race.
* **Flake-hunt every concurrency test.** At a 5–12% base rate, a single green run proves nothing. The
  bar used here was 25+ consecutive runs; one test was validated over 1500 request executions.
* **Agents declining part of a brief was right four times** — RT-05 refusing per-worker parallelism
  because `EngineSubstrate` holds shared per-instance state; the RT-04 agent refusing to cross the
  server/cli boundary; and two research agents correcting their own statistics before reporting.
  A spec written from outside the code does not always survive contact with it.
* **Long GPU runs outlive the agent that starts them and then die orphaned.** Four runs hit this.
  Aggressive per-item checkpointing plus an external watch on the checkpoint file is what makes it a
  non-event.
