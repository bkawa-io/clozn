{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "clozn.context-receipt-export.v1",
  "title": "Privacy-scoped immutable export of a context receipt",
  "type": "object",
  "required": [
    "schema_version",
    "run_id",
    "source_receipt_sha256",
    "privacy",
    "omitted_fields",
    "context_receipt"
  ],
  "properties": {
    "schema_version": {"const": "clozn.context-receipt-export.v1"},
    "run_id": {"type": "string", "minLength": 1},
    "source_receipt_sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "Canonical JSON SHA-256 of the stored source receipt before export-time privacy reduction."
    },
    "privacy": {
      "type": "string",
      "enum": ["full", "metadata_only", "hashes_only", "off"]
    },
    "omitted_fields": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Explicit field families withheld by the selected export tier."
    },
    "context_receipt": {
      "type": "object",
      "description": "A derived clozn.context-receipt.v1 document. The original run and receipt are never mutated."
    }
  },
  "additionalProperties": true
}
